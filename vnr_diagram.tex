\documentclass[tikz,border=10pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath}
\usetikzlibrary{arrows.meta,calc,positioning}

\begin{document}
\begin{tikzpicture}[
  >=Stealth,
  box/.style={draw, rounded corners=2pt, minimum width=1.3cm, minimum height=0.6cm,
              inner sep=3pt, font=\footnotesize},
  param/.style={draw, circle, minimum size=0.75cm, inner sep=1pt, font=\small},
  annot/.style={font=\tiny, text=gray},
  arr/.style={->, thick, black},
  radd/.style={->, thick, red!70},
  badd/.style={->, thick, blue!60},
]

% Title with collection info
\node at (5.5, 5.8) {\footnotesize collect statistics $s_0, m_0$};

% Parameters at top
\node[param, fill=red!8]  (V)   at (5.0, 4.8) {$V=Ws_0$};
\node[param, fill=blue!8] (b)   at (8.0, 4.8) {$b+Wm_0$};

% Main flow
\node[param]             (x)   at (0.5, 2.0) {$X$};
\node[box, fill=green!5] (bn)  at (3.0, 2.0) {BN};
\node[box]               (mul) at (5.0, 2.0) {Mult};
\node[box]               (add) at (8.0, 2.0) {Add};
\node[param]             (out) at (11.0, 2.0) {output};

% Annotation below BN
\node[annot] at (3.0, 1.2) {Update $s,m$};

% Main arrows
\draw[arr] (x) -- (bn);
\draw[arr] (bn) -- (mul);
\draw[arr] (mul) -- (add);
\draw[arr] (add) -- (out);

% Parameter arrows
\draw[radd] (V) -- (mul);
\draw[badd] (b) -- (add);

\end{tikzpicture}
\end{document}
